<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="movil.unicauca.peliculas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="En Cartelera" />
        <activity
            android:name=".DetailActivity"
            android:theme="@style/NoActionBar" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".EstrenosActivity" />
        <activity
            android:name=".Detail2Activity"
            android:theme="@style/NoActionBar" /> <!-- <activity android:name=".LoginActivity" /> -->
        <activity
            android:name=".AddActivity"
            android:label="@string/moviestoremember" />
        <activity
            android:name=".remember.RememberActivity"
            android:label="@string/recordatorios" />
        <activity android:name=".AddEstrenoActivity"></activity>
    </application>

</manifest>